# GitFlow - Guia de Uso

Este projeto utiliza o padrão **GitFlow** para gerenciamento de branches.

## 🌳 Estrutura de Branches

### Branches Principais

- **`main`** (ou `master`): Contém código em produção, sempre estável
- **`develop`**: Contém código de desenvolvimento, integração de features

### Branches de Suporte

- **`feature/*`**: Novas funcionalidades sendo desenvolvidas
- **`release/*`**: Preparação para novas versões
- **`hotfix/*`**: Correções urgentes em produção

## 📋 Fluxo de Trabalho

### 1. Iniciar uma Nova Feature

```bash
# Criar e mudar para branch de feature
git checkout develop
git pull origin develop
git checkout -b feature/nome-da-feature

# Desenvolver a feature...
git add .
git commit -m "feat: adiciona nova funcionalidade"

# Quando terminar, fazer merge de volta para develop
git checkout develop
git merge feature/nome-da-feature
git push origin develop
git branch -d feature/nome-da-feature
```

### 2. Criar uma Release

```bash
# Criar branch de release
git checkout develop
git pull origin develop
git checkout -b release/1.0.0

# Fazer ajustes finais, atualizar versão, etc.
git add .
git commit -m "chore: atualiza versão para 1.0.0"

# Finalizar release: merge para main e develop
git checkout main
git merge release/1.0.0
git tag -a v1.0.0 -m "Release 1.0.0"
git push origin main --tags

git checkout develop
git merge release/1.0.0
git push origin develop

git branch -d release/1.0.0
```

### 3. Criar um Hotfix

```bash
# Criar hotfix a partir de main
git checkout main
git pull origin main
git checkout -b hotfix/correcao-urgente

# Fazer correções...
git add .
git commit -m "fix: corrige problema crítico"

# Finalizar hotfix: merge para main e develop
git checkout main
git merge hotfix/correcao-urgente
git tag -a v1.0.1 -m "Hotfix 1.0.1"
git push origin main --tags

git checkout develop
git merge hotfix/correcao-urgente
git push origin develop

git branch -d hotfix/correcao-urgente
```

## 🏷️ Convenção de Commits

Use o padrão **Conventional Commits**:

- `feat:` Nova funcionalidade
- `fix:` Correção de bug
- `docs:` Mudanças na documentação
- `style:` Formatação, ponto e vírgula faltando, etc
- `refactor:` Refatoração de código
- `test:` Adicionando testes
- `chore:` Mudanças em build, dependências, etc

Exemplo:
```
feat: adiciona suporte para variáveis de ambiente
fix: corrige erro na ordenação de nós
docs: atualiza README com instruções de instalação
```

## 📝 Checklist Antes de Fazer Merge

- [ ] Código testado localmente
- [ ] Testes passando (`python src/test.py`)
- [ ] Sem erros de lint
- [ ] Documentação atualizada (se necessário)
- [ ] Commits seguem convenção
- [ ] Branch atualizada com develop/main

## 🚀 Scripts Auxiliares

Para facilitar o uso, criamos scripts auxiliares:

### Windows (PowerShell)

```powershell
# Criar nova feature
.\scripts\new-feature.ps1 nome-da-feature

# Finalizar feature
.\scripts\finish-feature.ps1 nome-da-feature

# Criar release
.\scripts\new-release.ps1 1.0.0

# Finalizar release
.\scripts\finish-release.ps1 1.0.0

# Criar hotfix
.\scripts\new-hotfix.ps1 correcao-urgente

# Finalizar hotfix
.\scripts\finish-hotfix.ps1 correcao-urgente
```

### Linux/Mac (Bash)

```bash
# Criar nova feature
./scripts/new-feature.sh nome-da-feature

# Finalizar feature
./scripts/finish-feature.sh nome-da-feature

# Criar release
./scripts/new-release.sh 1.0.0

# Finalizar release
./scripts/finish-release.sh 1.0.0

# Criar hotfix
./scripts/new-hotfix.sh correcao-urgente

# Finalizar hotfix
./scripts/finish-hotfix.sh correcao-urgente
```

## 🔧 Configuração Inicial

Se você está começando do zero:

```bash
# Criar branch develop
git checkout -b develop
git push -u origin develop

# Configurar branch padrão (opcional)
git config branch.develop.remote origin
git config branch.develop.merge refs/heads/develop
```

## 📚 Referências

- [GitFlow Workflow](https://www.atlassian.com/git/tutorials/comparing-workflows/gitflow-workflow)
- [Conventional Commits](https://www.conventionalcommits.org/)

